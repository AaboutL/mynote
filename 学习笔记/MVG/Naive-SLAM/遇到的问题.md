---
title: 遇到的问题
updated: 2022-12-11 15:58:44Z
created: 2022-11-17 03:04:24Z
---

1. IMU规格书中的加计噪声和陀螺仪噪声，需要乘以（IMU频率的开方）再使用
加计偏置和陀螺仪偏置的diffusion，需要除以（IMU频率的开方）再使用
2. 使用SolveEPnP方法求解相邻两帧之间的位姿，只能在一两帧之内使用，否则会迅速发散。导致位置t出现几千或者几万的值。
3. 判断IMU是否充分激励
	1. VINS-mono中的方法：考察线加速度的变化，计算滑窗中加速度的变化，只要标准差大于0.25，表示已经充分激励，足够初始化
		```
		//check imu observibility
			map<double, ImageFrame>::iterator frame_it;
			Vector3d sum_g;
			for (frame_it = all_image_frame.begin(), frame_it++; frame_it != all_image_frame.end(); frame_it++)
			{
				double dt = frame_it->second.pre_integration->sum_dt;
				Vector3d tmp_g = frame_it->second.pre_integration->delta_v / dt;
				sum_g += tmp_g;
			}
			Vector3d aver_g;
			aver_g = sum_g * 1.0 / ((int)all_image_frame.size() - 1);
			double var = 0;
			for (frame_it = all_image_frame.begin(), frame_it++; frame_it != all_image_frame.end(); frame_it++)
			{
				double dt = frame_it->second.pre_integration->sum_dt;
				Vector3d tmp_g = frame_it->second.pre_integration->delta_v / dt;
				var += (tmp_g - aver_g).transpose() * (tmp_g - aver_g);
				//cout << "frame g " << tmp_g.transpose() << endl;
			}
			var = sqrt(var / ((int)all_image_frame.size() - 1));
			//ROS_WARN("IMU variation %f!", var);
			if(var < 0.25)
      		{
            	ROS_INFO("IMU excitation not enouth!");
            	//return false;
        	}
		```
	
4. 里程计的优化：
	1. 先通过pnp初步求位姿
	2. 通过g2o优化位姿：对于非关键帧，只优化位姿和速度，3D点、imu的零偏都不优化；对于关键帧，同时优化位姿、速度、3D点、imu的零偏（此时关键帧之间的零偏应该单独设置）等。

5. Sophus::SO3d提示旋转矩阵不是正交阵。
6. 初始化与陀螺仪零偏估计有关的地方：
	1. IMU关于旋转的预积分，在body系中表示
	2. 相邻两帧图像之间的相对旋转，在world系中
7. 初始化调试：
	1. 把naive-slam的mJRbg保存，放到VINS-mono程序中验证是否能用。验证预积分中旋转相对偏置的雅可比是否使用正确
	2. 修改naive-slam的特征跟踪程序，保证更长的跟踪链。验证视觉初始化效果。
	3. 修改预积分中雅可比矩阵计算方式为VINS-mono的形式
	4. 把VINS-mono从中值积分改成欧拉积分，得到的陀螺仪偏置与带入naive-slam的mJRbg得到的陀螺仪偏置的值一样，但是相差一个负号。这样看来，mJRbg应该没错，但是这个负号应该如何理解？